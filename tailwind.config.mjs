const config = {
    purge: [],
    darkMode: "media", // or 'media' or 'class'
    theme: {
        extend: {
            // Complete with custom classes
        },
    },
    variants: {
        extend: {},
    },
    plugins: [],
}

export default config;
