import ComingSoon from '@/components/coming-soon';

const NotFound = () => {
    return (
        <ComingSoon
            className='h-screen'
            title={'not_found.title'}
            text={'not_found.text'}
        />
    );
};

export default NotFound;
