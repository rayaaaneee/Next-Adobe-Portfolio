import React from 'react'

import { cn } from '@/lib/utils'

export interface FooterIconProps {
    className?: string | null
}

const FooterIcon = ({ className }: FooterIconProps) => {
  return (
    <div className={cn(className)}>
      
    </div>
  )
}

export default FooterIcon;
